# pnpm records dependency build-script decisions here. These native deps
# (Next's image optimiser + resolver, and msw) need their build step. Without
# explicit decisions, `pnpm install --frozen-lockfile` fails in CI with
# ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  msw: true
  sharp: true
  unrs-resolver: true
